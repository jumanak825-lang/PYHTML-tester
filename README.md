Contains intro to PyHTML-simple generation library of html for python(tester.py).
It also has intro to templates-a pre-formatted file or structure used as a starting point for creating new content(testertemplate.py)
